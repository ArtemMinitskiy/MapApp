<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_authentication"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/mBackgroundColor"
    tools:context="com.example.artem.mapapp.MainActivity">

    <EditText android:layout_width="250dp"
        android:layout_height="50dp"
        android:id="@+id/login"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="145dp"
        android:hint="@string/login"
        android:textSize="30dp"/>
    <EditText android:layout_width="250dp"
        android:layout_height="50dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/login"
        android:hint="@string/password"
        android:textSize="28dp"
        android:id="@+id/password"
        />
    <Button android:layout_width="250dp"
        android:layout_height="60dp"
        android:hint="@string/autorize"
        android:textSize="30dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="70dp"
        android:background="@drawable/frame_brown_light"
        android:layout_below="@+id/password"
        android:onClick="addInDB"
        android:id="@+id/btn_sign_in"/>
</RelativeLayout>